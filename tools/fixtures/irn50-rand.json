{
  "fixture": "irn50-rand",
  "net": "upstream face_feature/irn50_pytorch.py irn50_pytorch (inception-resnet-50 variant, bn_eps=9.999999747378752e-06, asymmetric pads, final 512-d FC -> maxout over halves -> 256-d)",
  "weights": "irn50-rand.safetensors",
  "seed": 9012,
  "weight_recipe": "randn-kaiming-v1",
  "input_seed": 9013,
  "input_domain": "randint(0,256) / 256",
  "num_weight_tensors": 220,
  "notes": "output is the pre-normalization 256-d maxout embedding (eltwise max of bn_fc1[:, :256] and bn_fc1[:, 256:])",
  "input_file": "irn50-rand.npz#input",
  "output_files": [
    "irn50-rand.npz#embedding"
  ],
  "shapes": {
    "input": [
      1,
      3,
      128,
      128
    ],
    "embedding": [
      1,
      256
    ]
  },
  "dtype": "float32",
  "tolerance": 0.008063165283203125,
  "tolerance_rule": "max|delta| <= 1e-4 * max(1, max|output|), absolute"
}
