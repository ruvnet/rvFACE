node_modules/
dist/
# Only the redistributable base is committed (ADR-0003 + addendum): detector
# (MIT lineage), foamliu embedder (Apache-2.0, models/LICENSES.md), manifests.
# The landmark safetensors are never committed or deployed.
public/models/*
!public/models/detector-slim320.safetensors
!public/models/embedder-foamliu.safetensors
!public/models/embedder-foamliu.manifest.json
!public/models/landmark-mfn68.manifest.json
src/wasm/
